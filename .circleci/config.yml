# This config was automatically generated from your source code
# Stacks detected: cicd:github-actions:.github/workflows,deps:node:udagram-api-user
version: 2.1

jobs:
  build:
    # Build node project
    docker:
      - image: circleci/python:3.8
    environment:      
      DOCKERHUB_PASSWORD: $DOCKER_PASSWORD
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Build and push Docker image
          command: |
            docker build -t udagram-api-feed ./udagram-api-feed
            docker build -t udagram-api-user ./udagram-api-user
            docker build -t udagram-frontend ./udagram-frontend
            docker build -t reverseproxy ./udagram-reverseproxy
            docker tag udagram-api-feed athsueh/udagram-api-feed:latest
            docker tag udagram-api-user athsueh/udagram-api-user:latest
            docker tag udagram-frontend athsueh/udagram-frontend:latest
            docker tag reverseproxy athsueh/reverseproxy:latest
            docker login -u $DOCKER_USERNAME -p $DOCKER_PASSWORD
            docker push athsueh/udagram-api-feed:latest
            docker push athsueh/udagram-api-user:latest
            docker push athsueh/udagram-frontend:latest
            docker push athsueh/reverseproxy:latest

